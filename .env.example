# Required: Your private key for the target node (with 0x prefix)
PRIVATE_KEY=0x...

# Required: Provider ID on the target node
PROVIDER_ID=123

# Optional: RPC URL (defaults to Filecoin Calibration testnet)
# RPC_URL=https://api.calibration.node.glif.io/rpc/v1

# Optional: Source path for piece files (defaults to /filecoin-storage/piece)
# SOURCE_PATH=/filecoin-storage/piece

# Optional: Number of parallel uploads (defaults to 20)
# CONCURRENCY=20

# Optional: Log progress every N pieces (defaults to 50)
# LOG_INTERVAL=50
